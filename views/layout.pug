doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title= title
    link(rel='stylesheet', href='/output.css')
  body(class="bg-black text-white")
    nav(class="mb-10")
      .logo-container(class="flex justify-center gap-4 pt-10")
        img(src="/images/logo.png" class="w-24 h-auto")
        p(class="text-4xl") COLDSTORE
    block content
